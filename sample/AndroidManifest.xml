<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.supportanimator.sample"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7" 
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/appName"
        android:theme="@style/Theme.Sherlock">
        <activity
            android:name="com.supportanimator.sample.MainActivity"
            android:label="@string/appName" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="TransitionActivity" android:label="Fragment Transitions"/>
        <activity android:name="PagerActivity" android:label="Page Transformer"/>
        <activity android:name="PreferencesActivity" android:label="Preferences"/>
        <activity android:name="DrawerNavigationActivity" android:label="Drawer Layout"/>
        
    </application>

</manifest>
